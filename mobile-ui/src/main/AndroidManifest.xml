<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.kevincampos.insurancepolicies">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="me.kevincampos.insurancepolicies.InsurancePoliciesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".vehiclelist.VehicleListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".vehicledetail.VehicleDetailActivity"
            android:theme="@style/AppThemeNoActionBar" />

        <activity
            android:name=".policytransactions.PolicyTransactionsActivity"
            android:theme="@style/AppTheme" />

    </application>
</manifest>
